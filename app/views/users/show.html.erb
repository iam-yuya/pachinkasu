<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'info', user: @user %>
      <h2 class="mt-3">New Post</h2>
      <%= render 'posts/form', post: @post %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2>Posts</h2>
      <%=link_to"いいね一覧へ",favorites_user_path(@user) %>
      <%= render 'posts/index', posts: @posts %>
    </div>
  </div>
</div>